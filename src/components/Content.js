import React from "react";
import styled from "styled-components";

const Container = styled.div`
  height: 100px;
  width: 100px;
`;

const Content = () => {
  return <Container>본문</Container>;
};

export default Content;
